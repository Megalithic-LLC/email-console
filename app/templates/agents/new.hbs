<div class="container">

  <h4>Step 1: Install the On-Prem agent</h4>
  
  <BsTab as |tab|>
    <tab.pane @title="APT">
      <CodeBlock>
        # TODO register repository
        $ sudo apt-get install on-prem-emaild
      </CodeBlock>
    </tab.pane>
    <tab.pane @title="RPM">
      <CodeBlock>
        # TODO register repository
        $ sudo yum install on-prem-emaild
      </CodeBlock>
    </tab.pane>
  </BsTab>


  <h4>Step 2: Examine log to determine agent's node id</h4>
  <p>blah...</p>
  
  <CodeBlock>
    $ journalctl -u on-prem-emaild
    (timestamp) INFO On-Prem Email service started
    (timestamp) INFO Node id is u275l3hblmvu2345jj89
  </CodeBlock>

  
  <h4>Step 3: Claim the agent</h4>
  <p>
    Enter your agent's node id into this form to claim it.
  </p>

  
  <form {{action "claimAgent" model on="submit"}}>
    <div class="form-group">
      <label for="nodeID">Node ID</label>
      {{input id="nodeID" type="text" class="form-control" placeholder="Enter node ID" value=model.id}}
    </div>
    
    <button type="submit" class="btn btn-primary">Submit</button>
  </form>
</div>

{{outlet}}